{{>nav}}

<div class="container text-center">
  <h1 class="fw-light my-5">Estados</h1>
  {{#each otro}}
    {{#if fin}}
    <div class="card">
    <div class="card-header">
      Pago “Otros”
    </div>
    <div class="card-body">
      <blockquote class="blockquote mb-0">
        <p>Terminado.</p>
        <footer class="blockquote-footer">Ya terminamos <cite title="Source Title">Revisa tus cartones</cite></footer>
      </blockquote>
      <p>estado : {{estado}}</p>
    </div>
  </div>
    {{else}}
    <div class="card">
    <div class="card-header">
      Pago “Otros”
    </div>
    <div class="card-body">
      <blockquote class="blockquote mb-0">
        <p>En progreso</p>
        <footer class="blockquote-footer">Aun no termina <cite title="Source Title">Espera un poco</cite></footer>
        {{!-- <form action="/pagar/cancelar/otro" method="post">
          <input type="hidden" name="id" value="{{_id}}">
          {{!-- <button type="submit" class="btn btn-secondary">Cancelar orden</button> --}}
        </form> --}}
      </blockquote>
    </div>
  </div>
    {{/if}}
  {{/each}}
  {{#each trans}}
    {{#if fin}}
    <div class="card">
    <div class="card-header">
      Pago Transfrerencia
    </div>
    <div class="card-body">
      <blockquote class="blockquote mb-0">
        <p>Terminado.</p>
        <footer class="blockquote-footer">Ya terminamos <cite title="Source Title">Revisa tus cartones</cite></footer>
      </blockquote>
    </div>
  </div>
    {{else}}
    <div class="card">
    <div class="card-header">
      Pago Transfrerencia
    </div>
    <div class="card-body">
      <blockquote class="blockquote mb-0">
        <p>En progreso</p>
        <footer class="blockquote-footer">Aun no termina <cite title="Source Title">Espera un poco</cite></footer>
        {{!-- <form action="/pagar/cancelar/transfrerencia" method="post">
          <input type="hidden" name="id" value="{{_id}}">
          {{!-- <button type="submit" class="btn btn-secondary">Cancelar orden</button> --}}
        </form> --}}
      </blockquote>
       <p class="mt-3">Estado : {{#if estado}}
         {{estado}}
         {{else}}
         ~
         {{/if}}</p>
    </div>
  </div>
    {{/if}}
  {{/each}}
</div>

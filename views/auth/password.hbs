<style>
  @media(max-width: 800px){
    .col-6{
      width: 100%;
    }
  }
  .card{
    border:none
  }
</style>

<div class="container text-center h-100">
  <div class="row position-absolute h-100 align-content-center card">
    <div class="col-6">
      <h1 class="fw-light mt-5 mb-0"> Bingoloteando</h1>
      <p class="lead text-black m-0">En casa</p>
      <h1 class="fw-light mt-5">Recupera tu contraseña : {{user.nombre}} </h1>
      <div class="container px-5">
        <form action="/auth/password/create" method="post" class="needs-validation">
          <label for="inputPassword" class="fs-3 fw-light">Crea una contraseña Nueva</label>
              <input type="hidden" name="correo" value="{{user.correo}}">
              <input type="password" name="password" id="inputPassword" class="form-control" pattern="[a-zA-Z0-9(!¡¿?#$%&)]{8,}" required="" autocomplete="new-password">
              <label for="inputPassword" class="fw-light d-block">Minimo 8 caracteres</label>

              <label for="inputConfPassword" class="fs-3 fw-light mt-1">Confirma tu contraseña</label>
              <input type="password" name="p" id="inputConfPassword" class="form-control" required="" autocomplete="new-password">
              <button   class="btn btn-warning mt-5">Crear</button>
        </form>
        <p class="lead text-black mt-4 text-white">Para acceder a la página tienes que iniciar sesión o crearte una cuenta. Con eso podrás:  ¡Comprar, acceder y jugar tus cartones!, ¡Enterarte más de este evento, de los Jóvenes por un Mundo Unido y el Centro Mariápolis Cunaco! y ¡Poder ayudar a esta hermosa causa!</p>
        {{!-- <p class="lead text-black mt-0"><a href="/about"> Enterarte más de este evento </a></p> --}}
      </div>

      {{!-- <h5 class="fw-light po-2 my-5 bottom-100"> nesesitas ayuda? <a href="/help">Obtenla aquí</a></h5> --}}

    </div>
  </div>
</div>

<script>
  (function () {
  'use strict'

  // Fetch all the forms we want to apply custom Bootstrap validation styles to
  var forms = document.querySelectorAll('.needs-validation')

  // Loop over them and prevent submission
  Array.prototype.slice.call(forms)
    .forEach(function (form) {
      form.addEventListener('submit', function (event) {
        if($('#inputPassword')[0].value !== $('#inputConfPassword')[0].value){
          event.preventDefault()
          event.stopPropagation()
          $('#inputConfPassword')[0].value = ''
        }
        if (!form.checkValidity()) {
          event.preventDefault()
          event.stopPropagation()
        }
          form.classList.add('was-validated')
        

      }, false)
    })
})()
</script>